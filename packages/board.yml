esp32:
  board: $board
  variant: $variant
  flash_size: $flash_size
  framework:
    type: esp-idf
    sdkconfig_options:
      CONFIG_IDF_COMPONENT_MANAGER: "n"
    components:
      - name: tesla-ble
        source: https://github.com/jonootto/tesla-ble.git
        ref: v4.1.3
